import 'package:flutter/material.dart';
import 'top_common_nav.dart';

class IndexPage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
//      appBar: AppBar(title: Text('仿网易云音乐'),),
      child: Center(

      ),
    );
  }
}